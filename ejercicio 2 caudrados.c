#include <stdio.h>
#include <math.h>
int main ()
{
    float l,at,pt;
    l=7;

    at=(l*l)*5;
    pt=12*l;
    printf("el pt es:=%f",pt);
    printf("el at es:=%f",at);
    return 0;
}
